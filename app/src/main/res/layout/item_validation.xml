<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeColor="#E6E0EC"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- TIPO -->
        <TextView
            android:id="@+id/tvType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:letterSpacing="0.08"
            android:textAllCaps="true"
            android:textColor="#6B6B6B"
            android:textSize="11sp"
            android:textStyle="bold"
            tools:text="INCIDENCIA" />

        <!-- TÍTULO -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#1C1B1F"
            android:textSize="20sp"
            android:textStyle="bold"
            tools:text="Recepción de luminarias defectuosas" />

        <!-- DESCRIPCIÓN -->
        <TextView
            android:id="@+id/tvDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:lineSpacingExtra="2dp"
            android:textColor="#49454F"
            android:textSize="14sp"
            tools:text="Han llegado 4 luminarias rotas de ArkosLight." />

        <!-- BLOQUE METADATA -->
        <LinearLayout
            android:id="@+id/layoutMetadataContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:background="#F8F5FB"
            android:orientation="vertical"
            android:padding="12dp">

            <!-- HEADER -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Datos detectados"
                    android:textColor="#6750A4"
                    android:textSize="13sp"
                    android:textStyle="bold" />

                <!-- ELIMINAR TODO -->
                <ImageButton
                    android:id="@+id/btnRemoveMetadata"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Eliminar metadata"
                    android:padding="6dp"
                    android:src="@android:drawable/ic_menu_close_clear_cancel"
                    app:tint="#666666" />

            </LinearLayout>

            <!-- LISTA DINÁMICA -->
            <LinearLayout
                android:id="@+id/layoutMetadata"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="vertical"/>

            <!-- AÑADIR DATO -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAddMetadata"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Añadir dato"
                android:textAllCaps="false"
                android:textColor="#6750A4"
                app:icon="@android:drawable/ic_input_add"
                app:iconTint="#6750A4" />

        </LinearLayout>

        <!-- IMAGEN -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:orientation="vertical">

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/btnAddImage"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:contentDescription="Añadir imagen"
                app:srcCompat="@android:drawable/ic_menu_camera"
                app:fabSize="mini"
                app:backgroundTint="#6750A4"
                app:tint="@android:color/white"
                app:borderWidth="0dp"
                app:elevation="2dp" />

            <ImageView
                android:id="@+id/imgPreview"
                android:layout_width="match_parent"
                android:layout_height="180dp"
                android:layout_marginTop="10dp"
                android:background="#ECECEC"
                android:scaleType="centerCrop"
                android:visibility="gone" />

        </LinearLayout>

        <!-- ACCIONES -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:orientation="horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnReject"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Rechazar"
                android:textAllCaps="false"
                android:textColor="#B3261E" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAccept"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:text="Aceptar"
                android:textAllCaps="false"
                app:cornerRadius="24dp" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>