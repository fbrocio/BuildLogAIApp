<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuildLogAI"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.AddProjectActivity" />
        <activity android:name=".activities.ProjectDetailActivity" />
        <activity android:name=".activities.RecordingActivity" />
        <activity android:name=".activities.SummaryActivity" />
        <activity android:name=".activities.ValidationActivity" />
        <activity android:name=".activities.EditRecordActivity" />
        <activity android:name=".activities.SignupActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.GenerateReportActivity"/>
        <activity android:name=".activities.ReportResultActivity"/>
        <activity android:name=".activities.RecordDetailActivity"/>
        <activity
            android:name=".activities.VerifyEmailActivity"/>
        <activity
            android:name=".activities.ImageGalleryActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <activity
            android:name=".activities.LauncherActivity"
            android:theme="@style/Theme.BuildLogAI.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

</manifest>